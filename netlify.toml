[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback — Netlify Functions with custom `path` take precedence
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
